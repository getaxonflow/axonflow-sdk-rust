[package]
name = "axonflow-authzen-codegen"
version = "0.0.0"
edition = "2021"
rust-version = "1.78"
description = "Emits the Rust SDK's AuthZEN wire types from the platform's canonical surface artifact. Not published."
license = "MIT"
publish = false

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
