[package]
name = "fast-exit-delivery"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
axonflow-sdk-rust = { path = "../../.." }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
